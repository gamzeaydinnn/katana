version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5055:8080"
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__SqlServerConnection=Server=katana-sqlserver,1433;Database=KatanaDB;User Id=sa;Password=Admin00!S;TrustServerCertificate=True;
    networks:
      - katana-network
    volumes:
      - ./logs:/app/logs

  # DB servisleri kaldırıldı - host SQL Server kullanılıyor

networks:
  katana-network:
    driver: bridge

volumes:
  sqlserver-data:
    driver: local
